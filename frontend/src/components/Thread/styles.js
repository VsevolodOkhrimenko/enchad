import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  threadItem: {
    height: 70,
    padding: '20px 40px',
    display: 'block'
  }
}))

export default useStyles
